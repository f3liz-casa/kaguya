// SPDX-License-Identifier: MIT
// API endpoints for auth
// Generated by rescript-codegen-openapi
// DO NOT EDIT - This file is auto-generated



module Auth = {
    type postAuthAcceptRequest = {
      token: string,
    }

    let postAuthAcceptRequestSchema = S.object(s => {
        token: s.field("token", S.string),
      })

    type postAuthAcceptResponse = unit

    // auth/accept
    let postAuthAccept = (~body, ~fetch): promise<postAuthAcceptResponse> => {
      let validatedBody = postAuthAcceptRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/auth/accept",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postAuthSessionGenerateRequest = {
      appsecret: string,
    }

    let postAuthSessionGenerateRequestSchema = S.object(s => {
        appsecret: s.field("appSecret", S.string),
      })

    type postAuthSessionGenerateResponse = {
      token: string,
      url: string,
    }

    let postAuthSessionGenerateResponseSchema = S.object(s => {
        token: s.field("token", S.string),
        url: s.field("url", S.string),
      })

    // auth/session/generate
    let postAuthSessionGenerate = (~body, ~fetch): promise<postAuthSessionGenerateResponse> => {
      let validatedBody = postAuthSessionGenerateRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/auth/session/generate",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postAuthSessionGenerateResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postAuthSessionShowRequest = {
      token: string,
    }

    let postAuthSessionShowRequestSchema = S.object(s => {
        token: s.field("token", S.string),
      })

    type postAuthSessionShowResponse = {
      id: string,
      app: ComponentSchemas.App.t,
      token: string,
    }

    let postAuthSessionShowResponseSchema = S.object(s => {
        id: s.field("id", S.string),
        app: s.field("app", ComponentSchemas.App.schema),
        token: s.field("token", S.string),
      })

    // auth/session/show
    let postAuthSessionShow = (~body, ~fetch): promise<postAuthSessionShowResponse> => {
      let validatedBody = postAuthSessionShowRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/auth/session/show",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postAuthSessionShowResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postAuthSessionUserkeyRequest = {
      appsecret: string,
      token: string,
    }

    let postAuthSessionUserkeyRequestSchema = S.object(s => {
        appsecret: s.field("appSecret", S.string),
        token: s.field("token", S.string),
      })

    type postAuthSessionUserkeyResponse = {
      accesstoken: string,
      user: ComponentSchemas.Userdetailed.t,
    }

    let postAuthSessionUserkeyResponseSchema = S.object(s => {
        accesstoken: s.field("accessToken", S.string),
        user: s.field("user", ComponentSchemas.Userdetailed.schema),
      })

    // auth/session/userkey
    let postAuthSessionUserkey = (~body, ~fetch): promise<postAuthSessionUserkeyResponse> => {
      let validatedBody = postAuthSessionUserkeyRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/auth/session/userkey",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postAuthSessionUserkeyResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postMiauthGenTokenRequest = {
      session: JSON.t,
      name: option<JSON.t>,
      description: option<JSON.t>,
      iconurl: option<JSON.t>,
      permission: array<string>,
    }

    let postMiauthGenTokenRequestSchema = S.object(s => {
        session: s.field("session", S.json),
        name: s.fieldOr("name", S.nullableAsOption(S.json), None),
        description: s.fieldOr("description", S.nullableAsOption(S.json), None),
        iconurl: s.fieldOr("iconUrl", S.nullableAsOption(S.json), None),
        permission: s.field("permission", S.array(S.string)),
      })

    type postMiauthGenTokenResponse = {
      token: string,
    }

    let postMiauthGenTokenResponseSchema = S.object(s => {
        token: s.field("token", S.string),
      })

    // miauth/gen-token
    let postMiauthGenToken = (~body, ~fetch): promise<postMiauthGenTokenResponse> => {
      let validatedBody = postMiauthGenTokenRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/miauth/gen-token",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postMiauthGenTokenResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

}