// SPDX-License-Identifier: MIT
// API endpoints for channels
// Generated by rescript-codegen-openapi
// DO NOT EDIT - This file is auto-generated



module Channels = {
    type postChannelsCreateRequest = {
      name: string,
      description: option<JSON.t>,
      bannerid: option<JSON.t>,
      color: option<string>,
      issensitive: option<JSON.t>,
      allowrenotetoexternal: option<JSON.t>,
    }

    let postChannelsCreateRequestSchema = S.object(s => {
        name: s.field("name", S.string->S.min(1)->S.max(128)),
        description: s.fieldOr("description", S.nullableAsOption(S.json), None),
        bannerid: s.fieldOr("bannerId", S.nullableAsOption(S.json), None),
        color: s.fieldOr("color", S.nullableAsOption(S.string->S.min(1)->S.max(16)), None),
        issensitive: s.fieldOr("isSensitive", S.nullableAsOption(S.json), None),
        allowrenotetoexternal: s.fieldOr("allowRenoteToExternal", S.nullableAsOption(S.json), None),
      })

    type postChannelsCreateResponse = ComponentSchemas.Channel.t

    let postChannelsCreateResponseSchema = ComponentSchemas.Channel.schema

    // channels/create
    let postChannelsCreate = (~body, ~fetch): promise<postChannelsCreateResponse> => {
      let validatedBody = postChannelsCreateRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/channels/create",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postChannelsCreateResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postChannelsFavoriteRequest = {
      channelid: string,
    }

    let postChannelsFavoriteRequestSchema = S.object(s => {
        channelid: s.field("channelId", S.string),
      })

    type postChannelsFavoriteResponse = unit

    // channels/favorite
    let postChannelsFavorite = (~body, ~fetch): promise<postChannelsFavoriteResponse> => {
      let validatedBody = postChannelsFavoriteRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/channels/favorite",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postChannelsFeaturedResponse = array<ComponentSchemas.Channel.t>

    let postChannelsFeaturedResponseSchema = S.array(ComponentSchemas.Channel.schema)

    // channels/featured
    let postChannelsFeatured = (~fetch): promise<postChannelsFeaturedResponse> => {

      fetch(
        ~url="/channels/featured",
        ~method_="POST",
      )->Promise.then(response => {
      let validatedResponse = postChannelsFeaturedResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postChannelsFeaturedGamesResponse = array<ComponentSchemas.Channel.t>

    let postChannelsFeaturedGamesResponseSchema = S.array(ComponentSchemas.Channel.schema)

    // channels/featured-games
    let postChannelsFeaturedGames = (~fetch): promise<postChannelsFeaturedGamesResponse> => {

      fetch(
        ~url="/channels/featured-games",
        ~method_="POST",
      )->Promise.then(response => {
      let validatedResponse = postChannelsFeaturedGamesResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postChannelsFollowRequest = {
      channelid: string,
    }

    let postChannelsFollowRequestSchema = S.object(s => {
        channelid: s.field("channelId", S.string),
      })

    type postChannelsFollowResponse = unit

    // channels/follow
    let postChannelsFollow = (~body, ~fetch): promise<postChannelsFollowResponse> => {
      let validatedBody = postChannelsFollowRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/channels/follow",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postChannelsFollowedRequest = {
      sinceid: option<string>,
      untilid: option<string>,
      limit: option<int>,
    }

    let postChannelsFollowedRequestSchema = S.object(s => {
        sinceid: s.fieldOr("sinceId", S.nullableAsOption(S.string), None),
        untilid: s.fieldOr("untilId", S.nullableAsOption(S.string), None),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(100)), None),
      })

    type postChannelsFollowedResponse = array<ComponentSchemas.Channel.t>

    let postChannelsFollowedResponseSchema = S.array(ComponentSchemas.Channel.schema)

    // channels/followed
    let postChannelsFollowed = (~body, ~fetch): promise<postChannelsFollowedResponse> => {
      let validatedBody = postChannelsFollowedRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/channels/followed",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postChannelsFollowedResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postChannelsMyFavoritesResponse = array<ComponentSchemas.Channel.t>

    let postChannelsMyFavoritesResponseSchema = S.array(ComponentSchemas.Channel.schema)

    // channels/my-favorites
    let postChannelsMyFavorites = (~fetch): promise<postChannelsMyFavoritesResponse> => {

      fetch(
        ~url="/channels/my-favorites",
        ~method_="POST",
      )->Promise.then(response => {
      let validatedResponse = postChannelsMyFavoritesResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postChannelsOwnedRequest = {
      sinceid: option<string>,
      untilid: option<string>,
      limit: option<int>,
    }

    let postChannelsOwnedRequestSchema = S.object(s => {
        sinceid: s.fieldOr("sinceId", S.nullableAsOption(S.string), None),
        untilid: s.fieldOr("untilId", S.nullableAsOption(S.string), None),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(100)), None),
      })

    type postChannelsOwnedResponse = array<ComponentSchemas.Channel.t>

    let postChannelsOwnedResponseSchema = S.array(ComponentSchemas.Channel.schema)

    // channels/owned
    let postChannelsOwned = (~body, ~fetch): promise<postChannelsOwnedResponse> => {
      let validatedBody = postChannelsOwnedRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/channels/owned",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postChannelsOwnedResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postChannelsSearchRequest = {
      query: string,
      @as("type") type_: option<string>,
      sinceid: option<string>,
      untilid: option<string>,
      limit: option<int>,
    }

    let postChannelsSearchRequestSchema = S.object(s => {
        query: s.field("query", S.string),
        type_: s.fieldOr("type", S.nullableAsOption(S.string), None),
        sinceid: s.fieldOr("sinceId", S.nullableAsOption(S.string), None),
        untilid: s.fieldOr("untilId", S.nullableAsOption(S.string), None),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(100)), None),
      })

    type postChannelsSearchResponse = array<ComponentSchemas.Channel.t>

    let postChannelsSearchResponseSchema = S.array(ComponentSchemas.Channel.schema)

    // channels/search
    let postChannelsSearch = (~body, ~fetch): promise<postChannelsSearchResponse> => {
      let validatedBody = postChannelsSearchRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/channels/search",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postChannelsSearchResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postChannelsShowRequest = {
      channelid: string,
    }

    let postChannelsShowRequestSchema = S.object(s => {
        channelid: s.field("channelId", S.string),
      })

    type postChannelsShowResponse = ComponentSchemas.Channel.t

    let postChannelsShowResponseSchema = ComponentSchemas.Channel.schema

    // channels/show
    let postChannelsShow = (~body, ~fetch): promise<postChannelsShowResponse> => {
      let validatedBody = postChannelsShowRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/channels/show",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postChannelsShowResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postChannelsUnfavoriteRequest = {
      channelid: string,
    }

    let postChannelsUnfavoriteRequestSchema = S.object(s => {
        channelid: s.field("channelId", S.string),
      })

    type postChannelsUnfavoriteResponse = unit

    // channels/unfavorite
    let postChannelsUnfavorite = (~body, ~fetch): promise<postChannelsUnfavoriteResponse> => {
      let validatedBody = postChannelsUnfavoriteRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/channels/unfavorite",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postChannelsUnfollowRequest = {
      channelid: string,
    }

    let postChannelsUnfollowRequestSchema = S.object(s => {
        channelid: s.field("channelId", S.string),
      })

    type postChannelsUnfollowResponse = unit

    // channels/unfollow
    let postChannelsUnfollow = (~body, ~fetch): promise<postChannelsUnfollowResponse> => {
      let validatedBody = postChannelsUnfollowRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/channels/unfollow",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postChannelsUpdateRequest = {
      channelid: string,
      name: option<string>,
      description: option<JSON.t>,
      bannerid: option<JSON.t>,
      isarchived: option<JSON.t>,
      pinnednoteids: option<array<string>>,
      color: option<string>,
      issensitive: option<JSON.t>,
      allowrenotetoexternal: option<JSON.t>,
    }

    let postChannelsUpdateRequestSchema = S.object(s => {
        channelid: s.field("channelId", S.string),
        name: s.fieldOr("name", S.nullableAsOption(S.string->S.min(1)->S.max(128)), None),
        description: s.fieldOr("description", S.nullableAsOption(S.json), None),
        bannerid: s.fieldOr("bannerId", S.nullableAsOption(S.json), None),
        isarchived: s.fieldOr("isArchived", S.nullableAsOption(S.json), None),
        pinnednoteids: s.fieldOr("pinnedNoteIds", S.nullableAsOption(S.array(S.string)), None),
        color: s.fieldOr("color", S.nullableAsOption(S.string->S.min(1)->S.max(16)), None),
        issensitive: s.fieldOr("isSensitive", S.nullableAsOption(S.json), None),
        allowrenotetoexternal: s.fieldOr("allowRenoteToExternal", S.nullableAsOption(S.json), None),
      })

    type postChannelsUpdateResponse = ComponentSchemas.Channel.t

    let postChannelsUpdateResponseSchema = ComponentSchemas.Channel.schema

    // channels/update
    let postChannelsUpdate = (~body, ~fetch): promise<postChannelsUpdateResponse> => {
      let validatedBody = postChannelsUpdateRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/channels/update",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postChannelsUpdateResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

}