// SPDX-License-Identifier: MIT
// API endpoints for hashtags
// Generated by rescript-codegen-openapi
// DO NOT EDIT - This file is auto-generated



module Hashtags = {
    type postHashtagsListRequest = {
      limit: option<int>,
      attachedtouseronly: option<bool>,
      attachedtolocaluseronly: option<bool>,
      attachedtoremoteuseronly: option<bool>,
      sort: string,
    }

    let postHashtagsListRequestSchema = S.object(s => {
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(100)), None),
        attachedtouseronly: s.fieldOr("attachedToUserOnly", S.nullableAsOption(S.bool), None),
        attachedtolocaluseronly: s.fieldOr("attachedToLocalUserOnly", S.nullableAsOption(S.bool), None),
        attachedtoremoteuseronly: s.fieldOr("attachedToRemoteUserOnly", S.nullableAsOption(S.bool), None),
        sort: s.field("sort", S.string),
      })

    type postHashtagsListResponse = array<ComponentSchemas.Hashtag.t>

    let postHashtagsListResponseSchema = S.array(ComponentSchemas.Hashtag.schema)

    // hashtags/list
    let postHashtagsList = (~body, ~fetch): promise<postHashtagsListResponse> => {
      let validatedBody = postHashtagsListRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/hashtags/list",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postHashtagsListResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postHashtagsSearchRequest = {
      limit: option<int>,
      query: string,
      offset: option<int>,
    }

    let postHashtagsSearchRequestSchema = S.object(s => {
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(100)), None),
        query: s.field("query", S.string),
        offset: s.fieldOr("offset", S.nullableAsOption(S.int), None),
      })

    type postHashtagsSearchResponse = array<string>

    let postHashtagsSearchResponseSchema = S.array(S.string)

    // hashtags/search
    let postHashtagsSearch = (~body, ~fetch): promise<postHashtagsSearchResponse> => {
      let validatedBody = postHashtagsSearchRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/hashtags/search",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postHashtagsSearchResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postHashtagsShowRequest = {
      tag: string,
    }

    let postHashtagsShowRequestSchema = S.object(s => {
        tag: s.field("tag", S.string),
      })

    type postHashtagsShowResponse = ComponentSchemas.Hashtag.t

    let postHashtagsShowResponseSchema = ComponentSchemas.Hashtag.schema

    // hashtags/show
    let postHashtagsShow = (~body, ~fetch): promise<postHashtagsShowResponse> => {
      let validatedBody = postHashtagsShowRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/hashtags/show",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postHashtagsShowResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type getHashtagsTrendResponse = array<JSON.t>

    let getHashtagsTrendResponseSchema = S.array(S.json)

    // hashtags/trend
    let getHashtagsTrend = (~fetch): promise<getHashtagsTrendResponse> => {

      fetch(
        ~url="/hashtags/trend",
        ~method_="GET",
      )->Promise.then(response => {
      let validatedResponse = getHashtagsTrendResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postHashtagsTrendResponse = array<JSON.t>

    let postHashtagsTrendResponseSchema = S.array(S.json)

    // hashtags/trend
    let postHashtagsTrend = (~fetch): promise<postHashtagsTrendResponse> => {

      fetch(
        ~url="/hashtags/trend",
        ~method_="POST",
      )->Promise.then(response => {
      let validatedResponse = postHashtagsTrendResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postHashtagsUsersRequest = {
      tag: string,
      limit: option<int>,
      sort: string,
      state: option<string>,
      origin: option<string>,
      sinceid: option<string>,
      untilid: option<string>,
    }

    let postHashtagsUsersRequestSchema = S.object(s => {
        tag: s.field("tag", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(100)), None),
        sort: s.field("sort", S.string),
        state: s.fieldOr("state", S.nullableAsOption(S.string), None),
        origin: s.fieldOr("origin", S.nullableAsOption(S.string), None),
        sinceid: s.fieldOr("sinceId", S.nullableAsOption(S.string), None),
        untilid: s.fieldOr("untilId", S.nullableAsOption(S.string), None),
      })

    type postHashtagsUsersResponse = array<ComponentSchemas.Userdetailed.t>

    let postHashtagsUsersResponseSchema = S.array(ComponentSchemas.Userdetailed.schema)

    // hashtags/users
    let postHashtagsUsers = (~body, ~fetch): promise<postHashtagsUsersResponse> => {
      let validatedBody = postHashtagsUsersRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/hashtags/users",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postHashtagsUsersResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

}