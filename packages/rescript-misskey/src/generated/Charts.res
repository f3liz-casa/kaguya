// SPDX-License-Identifier: MIT
// API endpoints for charts
// Generated by rescript-codegen-openapi
// DO NOT EDIT - This file is auto-generated



module Charts = {
    type getChartsActiveUsersRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
    }

    let getChartsActiveUsersRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
      })

    type getChartsActiveUsersResponse = {
      readwrite: array<float>,
      read: array<float>,
      write: array<float>,
      registeredwithinweek: array<float>,
      registeredwithinmonth: array<float>,
      registeredwithinyear: array<float>,
      registeredoutsideweek: array<float>,
      registeredoutsidemonth: array<float>,
      registeredoutsideyear: array<float>,
    }

    let getChartsActiveUsersResponseSchema = S.object(s => {
        readwrite: s.field("readWrite", S.array(S.float)),
        read: s.field("read", S.array(S.float)),
        write: s.field("write", S.array(S.float)),
        registeredwithinweek: s.field("registeredWithinWeek", S.array(S.float)),
        registeredwithinmonth: s.field("registeredWithinMonth", S.array(S.float)),
        registeredwithinyear: s.field("registeredWithinYear", S.array(S.float)),
        registeredoutsideweek: s.field("registeredOutsideWeek", S.array(S.float)),
        registeredoutsidemonth: s.field("registeredOutsideMonth", S.array(S.float)),
        registeredoutsideyear: s.field("registeredOutsideYear", S.array(S.float)),
      })

    // charts/active-users
    let getChartsActiveUsers = (~body, ~fetch): promise<getChartsActiveUsersResponse> => {
      let validatedBody = getChartsActiveUsersRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/active-users",
        ~method_="GET",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = getChartsActiveUsersResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postChartsActiveUsersRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
    }

    let postChartsActiveUsersRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
      })

    type postChartsActiveUsersResponse = {
      readwrite: array<float>,
      read: array<float>,
      write: array<float>,
      registeredwithinweek: array<float>,
      registeredwithinmonth: array<float>,
      registeredwithinyear: array<float>,
      registeredoutsideweek: array<float>,
      registeredoutsidemonth: array<float>,
      registeredoutsideyear: array<float>,
    }

    let postChartsActiveUsersResponseSchema = S.object(s => {
        readwrite: s.field("readWrite", S.array(S.float)),
        read: s.field("read", S.array(S.float)),
        write: s.field("write", S.array(S.float)),
        registeredwithinweek: s.field("registeredWithinWeek", S.array(S.float)),
        registeredwithinmonth: s.field("registeredWithinMonth", S.array(S.float)),
        registeredwithinyear: s.field("registeredWithinYear", S.array(S.float)),
        registeredoutsideweek: s.field("registeredOutsideWeek", S.array(S.float)),
        registeredoutsidemonth: s.field("registeredOutsideMonth", S.array(S.float)),
        registeredoutsideyear: s.field("registeredOutsideYear", S.array(S.float)),
      })

    // charts/active-users
    let postChartsActiveUsers = (~body, ~fetch): promise<postChartsActiveUsersResponse> => {
      let validatedBody = postChartsActiveUsersRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/active-users",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postChartsActiveUsersResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type getChartsApRequestRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
    }

    let getChartsApRequestRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
      })

    type getChartsApRequestResponse = {
      deliverfailed: array<float>,
      deliversucceeded: array<float>,
      inboxreceived: array<float>,
    }

    let getChartsApRequestResponseSchema = S.object(s => {
        deliverfailed: s.field("deliverFailed", S.array(S.float)),
        deliversucceeded: s.field("deliverSucceeded", S.array(S.float)),
        inboxreceived: s.field("inboxReceived", S.array(S.float)),
      })

    // charts/ap-request
    let getChartsApRequest = (~body, ~fetch): promise<getChartsApRequestResponse> => {
      let validatedBody = getChartsApRequestRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/ap-request",
        ~method_="GET",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = getChartsApRequestResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postChartsApRequestRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
    }

    let postChartsApRequestRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
      })

    type postChartsApRequestResponse = {
      deliverfailed: array<float>,
      deliversucceeded: array<float>,
      inboxreceived: array<float>,
    }

    let postChartsApRequestResponseSchema = S.object(s => {
        deliverfailed: s.field("deliverFailed", S.array(S.float)),
        deliversucceeded: s.field("deliverSucceeded", S.array(S.float)),
        inboxreceived: s.field("inboxReceived", S.array(S.float)),
      })

    // charts/ap-request
    let postChartsApRequest = (~body, ~fetch): promise<postChartsApRequestResponse> => {
      let validatedBody = postChartsApRequestRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/ap-request",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postChartsApRequestResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type getChartsDriveRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
    }

    let getChartsDriveRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
      })

    type getChartsDriveResponse = {
      local: JSON.t,
      remote: JSON.t,
    }

    let getChartsDriveResponseSchema = S.object(s => {
        local: s.field("local", S.json),
        remote: s.field("remote", S.json),
      })

    // charts/drive
    let getChartsDrive = (~body, ~fetch): promise<getChartsDriveResponse> => {
      let validatedBody = getChartsDriveRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/drive",
        ~method_="GET",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = getChartsDriveResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postChartsDriveRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
    }

    let postChartsDriveRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
      })

    type postChartsDriveResponse = {
      local: JSON.t,
      remote: JSON.t,
    }

    let postChartsDriveResponseSchema = S.object(s => {
        local: s.field("local", S.json),
        remote: s.field("remote", S.json),
      })

    // charts/drive
    let postChartsDrive = (~body, ~fetch): promise<postChartsDriveResponse> => {
      let validatedBody = postChartsDriveRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/drive",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postChartsDriveResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type getChartsFederationRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
    }

    let getChartsFederationRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
      })

    type getChartsFederationResponse = {
      deliveredinstances: array<float>,
      inboxinstances: array<float>,
      stalled: array<float>,
      sub: array<float>,
      pub: array<float>,
      pubsub: array<float>,
      subactive: array<float>,
      pubactive: array<float>,
    }

    let getChartsFederationResponseSchema = S.object(s => {
        deliveredinstances: s.field("deliveredInstances", S.array(S.float)),
        inboxinstances: s.field("inboxInstances", S.array(S.float)),
        stalled: s.field("stalled", S.array(S.float)),
        sub: s.field("sub", S.array(S.float)),
        pub: s.field("pub", S.array(S.float)),
        pubsub: s.field("pubsub", S.array(S.float)),
        subactive: s.field("subActive", S.array(S.float)),
        pubactive: s.field("pubActive", S.array(S.float)),
      })

    // charts/federation
    let getChartsFederation = (~body, ~fetch): promise<getChartsFederationResponse> => {
      let validatedBody = getChartsFederationRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/federation",
        ~method_="GET",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = getChartsFederationResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postChartsFederationRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
    }

    let postChartsFederationRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
      })

    type postChartsFederationResponse = {
      deliveredinstances: array<float>,
      inboxinstances: array<float>,
      stalled: array<float>,
      sub: array<float>,
      pub: array<float>,
      pubsub: array<float>,
      subactive: array<float>,
      pubactive: array<float>,
    }

    let postChartsFederationResponseSchema = S.object(s => {
        deliveredinstances: s.field("deliveredInstances", S.array(S.float)),
        inboxinstances: s.field("inboxInstances", S.array(S.float)),
        stalled: s.field("stalled", S.array(S.float)),
        sub: s.field("sub", S.array(S.float)),
        pub: s.field("pub", S.array(S.float)),
        pubsub: s.field("pubsub", S.array(S.float)),
        subactive: s.field("subActive", S.array(S.float)),
        pubactive: s.field("pubActive", S.array(S.float)),
      })

    // charts/federation
    let postChartsFederation = (~body, ~fetch): promise<postChartsFederationResponse> => {
      let validatedBody = postChartsFederationRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/federation",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postChartsFederationResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type getChartsInstanceRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
      host: string,
    }

    let getChartsInstanceRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
        host: s.field("host", S.string),
      })

    type getChartsInstanceResponse = {
      requests: JSON.t,
      notes: JSON.t,
      users: JSON.t,
      following: JSON.t,
      followers: JSON.t,
      drive: JSON.t,
    }

    let getChartsInstanceResponseSchema = S.object(s => {
        requests: s.field("requests", S.json),
        notes: s.field("notes", S.json),
        users: s.field("users", S.json),
        following: s.field("following", S.json),
        followers: s.field("followers", S.json),
        drive: s.field("drive", S.json),
      })

    // charts/instance
    let getChartsInstance = (~body, ~fetch): promise<getChartsInstanceResponse> => {
      let validatedBody = getChartsInstanceRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/instance",
        ~method_="GET",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = getChartsInstanceResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postChartsInstanceRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
      host: string,
    }

    let postChartsInstanceRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
        host: s.field("host", S.string),
      })

    type postChartsInstanceResponse = {
      requests: JSON.t,
      notes: JSON.t,
      users: JSON.t,
      following: JSON.t,
      followers: JSON.t,
      drive: JSON.t,
    }

    let postChartsInstanceResponseSchema = S.object(s => {
        requests: s.field("requests", S.json),
        notes: s.field("notes", S.json),
        users: s.field("users", S.json),
        following: s.field("following", S.json),
        followers: s.field("followers", S.json),
        drive: s.field("drive", S.json),
      })

    // charts/instance
    let postChartsInstance = (~body, ~fetch): promise<postChartsInstanceResponse> => {
      let validatedBody = postChartsInstanceRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/instance",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postChartsInstanceResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type getChartsNotesRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
    }

    let getChartsNotesRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
      })

    type getChartsNotesResponse = {
      local: JSON.t,
      remote: JSON.t,
    }

    let getChartsNotesResponseSchema = S.object(s => {
        local: s.field("local", S.json),
        remote: s.field("remote", S.json),
      })

    // charts/notes
    let getChartsNotes = (~body, ~fetch): promise<getChartsNotesResponse> => {
      let validatedBody = getChartsNotesRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/notes",
        ~method_="GET",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = getChartsNotesResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postChartsNotesRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
    }

    let postChartsNotesRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
      })

    type postChartsNotesResponse = {
      local: JSON.t,
      remote: JSON.t,
    }

    let postChartsNotesResponseSchema = S.object(s => {
        local: s.field("local", S.json),
        remote: s.field("remote", S.json),
      })

    // charts/notes
    let postChartsNotes = (~body, ~fetch): promise<postChartsNotesResponse> => {
      let validatedBody = postChartsNotesRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/notes",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postChartsNotesResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type getChartsUserDriveRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
      userid: string,
    }

    let getChartsUserDriveRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
        userid: s.field("userId", S.string),
      })

    type getChartsUserDriveResponse = {
      totalcount: array<float>,
      totalsize: array<float>,
      inccount: array<float>,
      incsize: array<float>,
      deccount: array<float>,
      decsize: array<float>,
    }

    let getChartsUserDriveResponseSchema = S.object(s => {
        totalcount: s.field("totalCount", S.array(S.float)),
        totalsize: s.field("totalSize", S.array(S.float)),
        inccount: s.field("incCount", S.array(S.float)),
        incsize: s.field("incSize", S.array(S.float)),
        deccount: s.field("decCount", S.array(S.float)),
        decsize: s.field("decSize", S.array(S.float)),
      })

    // charts/user/drive
    let getChartsUserDrive = (~body, ~fetch): promise<getChartsUserDriveResponse> => {
      let validatedBody = getChartsUserDriveRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/user/drive",
        ~method_="GET",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = getChartsUserDriveResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postChartsUserDriveRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
      userid: string,
    }

    let postChartsUserDriveRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
        userid: s.field("userId", S.string),
      })

    type postChartsUserDriveResponse = {
      totalcount: array<float>,
      totalsize: array<float>,
      inccount: array<float>,
      incsize: array<float>,
      deccount: array<float>,
      decsize: array<float>,
    }

    let postChartsUserDriveResponseSchema = S.object(s => {
        totalcount: s.field("totalCount", S.array(S.float)),
        totalsize: s.field("totalSize", S.array(S.float)),
        inccount: s.field("incCount", S.array(S.float)),
        incsize: s.field("incSize", S.array(S.float)),
        deccount: s.field("decCount", S.array(S.float)),
        decsize: s.field("decSize", S.array(S.float)),
      })

    // charts/user/drive
    let postChartsUserDrive = (~body, ~fetch): promise<postChartsUserDriveResponse> => {
      let validatedBody = postChartsUserDriveRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/user/drive",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postChartsUserDriveResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type getChartsUserFollowingRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
      userid: string,
    }

    let getChartsUserFollowingRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
        userid: s.field("userId", S.string),
      })

    type getChartsUserFollowingResponse = {
      local: JSON.t,
      remote: JSON.t,
    }

    let getChartsUserFollowingResponseSchema = S.object(s => {
        local: s.field("local", S.json),
        remote: s.field("remote", S.json),
      })

    // charts/user/following
    let getChartsUserFollowing = (~body, ~fetch): promise<getChartsUserFollowingResponse> => {
      let validatedBody = getChartsUserFollowingRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/user/following",
        ~method_="GET",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = getChartsUserFollowingResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postChartsUserFollowingRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
      userid: string,
    }

    let postChartsUserFollowingRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
        userid: s.field("userId", S.string),
      })

    type postChartsUserFollowingResponse = {
      local: JSON.t,
      remote: JSON.t,
    }

    let postChartsUserFollowingResponseSchema = S.object(s => {
        local: s.field("local", S.json),
        remote: s.field("remote", S.json),
      })

    // charts/user/following
    let postChartsUserFollowing = (~body, ~fetch): promise<postChartsUserFollowingResponse> => {
      let validatedBody = postChartsUserFollowingRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/user/following",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postChartsUserFollowingResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type getChartsUserNotesRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
      userid: string,
    }

    let getChartsUserNotesRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
        userid: s.field("userId", S.string),
      })

    type getChartsUserNotesResponse = {
      total: array<float>,
      inc: array<float>,
      dec: array<float>,
      diffs: JSON.t,
    }

    let getChartsUserNotesResponseSchema = S.object(s => {
        total: s.field("total", S.array(S.float)),
        inc: s.field("inc", S.array(S.float)),
        dec: s.field("dec", S.array(S.float)),
        diffs: s.field("diffs", S.json),
      })

    // charts/user/notes
    let getChartsUserNotes = (~body, ~fetch): promise<getChartsUserNotesResponse> => {
      let validatedBody = getChartsUserNotesRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/user/notes",
        ~method_="GET",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = getChartsUserNotesResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postChartsUserNotesRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
      userid: string,
    }

    let postChartsUserNotesRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
        userid: s.field("userId", S.string),
      })

    type postChartsUserNotesResponse = {
      total: array<float>,
      inc: array<float>,
      dec: array<float>,
      diffs: JSON.t,
    }

    let postChartsUserNotesResponseSchema = S.object(s => {
        total: s.field("total", S.array(S.float)),
        inc: s.field("inc", S.array(S.float)),
        dec: s.field("dec", S.array(S.float)),
        diffs: s.field("diffs", S.json),
      })

    // charts/user/notes
    let postChartsUserNotes = (~body, ~fetch): promise<postChartsUserNotesResponse> => {
      let validatedBody = postChartsUserNotesRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/user/notes",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postChartsUserNotesResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type getChartsUserPvRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
      userid: string,
    }

    let getChartsUserPvRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
        userid: s.field("userId", S.string),
      })

    type getChartsUserPvResponse = {
      upv: JSON.t,
      pv: JSON.t,
    }

    let getChartsUserPvResponseSchema = S.object(s => {
        upv: s.field("upv", S.json),
        pv: s.field("pv", S.json),
      })

    // charts/user/pv
    let getChartsUserPv = (~body, ~fetch): promise<getChartsUserPvResponse> => {
      let validatedBody = getChartsUserPvRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/user/pv",
        ~method_="GET",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = getChartsUserPvResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postChartsUserPvRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
      userid: string,
    }

    let postChartsUserPvRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
        userid: s.field("userId", S.string),
      })

    type postChartsUserPvResponse = {
      upv: JSON.t,
      pv: JSON.t,
    }

    let postChartsUserPvResponseSchema = S.object(s => {
        upv: s.field("upv", S.json),
        pv: s.field("pv", S.json),
      })

    // charts/user/pv
    let postChartsUserPv = (~body, ~fetch): promise<postChartsUserPvResponse> => {
      let validatedBody = postChartsUserPvRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/user/pv",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postChartsUserPvResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type getChartsUserReactionsRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
      userid: string,
    }

    let getChartsUserReactionsRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
        userid: s.field("userId", S.string),
      })

    type getChartsUserReactionsResponse = {
      local: JSON.t,
      remote: JSON.t,
    }

    let getChartsUserReactionsResponseSchema = S.object(s => {
        local: s.field("local", S.json),
        remote: s.field("remote", S.json),
      })

    // charts/user/reactions
    let getChartsUserReactions = (~body, ~fetch): promise<getChartsUserReactionsResponse> => {
      let validatedBody = getChartsUserReactionsRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/user/reactions",
        ~method_="GET",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = getChartsUserReactionsResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postChartsUserReactionsRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
      userid: string,
    }

    let postChartsUserReactionsRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
        userid: s.field("userId", S.string),
      })

    type postChartsUserReactionsResponse = {
      local: JSON.t,
      remote: JSON.t,
    }

    let postChartsUserReactionsResponseSchema = S.object(s => {
        local: s.field("local", S.json),
        remote: s.field("remote", S.json),
      })

    // charts/user/reactions
    let postChartsUserReactions = (~body, ~fetch): promise<postChartsUserReactionsResponse> => {
      let validatedBody = postChartsUserReactionsRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/user/reactions",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postChartsUserReactionsResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type getChartsUsersRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
    }

    let getChartsUsersRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
      })

    type getChartsUsersResponse = {
      local: JSON.t,
      remote: JSON.t,
    }

    let getChartsUsersResponseSchema = S.object(s => {
        local: s.field("local", S.json),
        remote: s.field("remote", S.json),
      })

    // charts/users
    let getChartsUsers = (~body, ~fetch): promise<getChartsUsersResponse> => {
      let validatedBody = getChartsUsersRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/users",
        ~method_="GET",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = getChartsUsersResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postChartsUsersRequest = {
      span: string,
      limit: option<int>,
      offset: option<JSON.t>,
    }

    let postChartsUsersRequestSchema = S.object(s => {
        span: s.field("span", S.string),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(500)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.json), None),
      })

    type postChartsUsersResponse = {
      local: JSON.t,
      remote: JSON.t,
    }

    let postChartsUsersResponseSchema = S.object(s => {
        local: s.field("local", S.json),
        remote: s.field("remote", S.json),
      })

    // charts/users
    let postChartsUsers = (~body, ~fetch): promise<postChartsUsersResponse> => {
      let validatedBody = postChartsUsersRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/charts/users",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postChartsUsersResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

}