// SPDX-License-Identifier: MIT
// API endpoints for drive
// Generated by rescript-codegen-openapi
// DO NOT EDIT - This file is auto-generated



module Drive = {
    type postDriveResponse = {
      capacity: float,
      usage: float,
    }

    let postDriveResponseSchema = S.object(s => {
        capacity: s.field("capacity", S.float),
        usage: s.field("usage", S.float),
      })

    // drive
    let postDrive = (~fetch): promise<postDriveResponse> => {

      fetch(
        ~url="/drive",
        ~method_="POST",
      )->Promise.then(response => {
      let validatedResponse = postDriveResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postDriveFilesRequest = {
      limit: option<int>,
      sinceid: option<string>,
      untilid: option<string>,
      folderid: option<JSON.t>,
      @as("type") type_: option<JSON.t>,
      sort: option<JSON.t>,
    }

    let postDriveFilesRequestSchema = S.object(s => {
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(100)), None),
        sinceid: s.fieldOr("sinceId", S.nullableAsOption(S.string), None),
        untilid: s.fieldOr("untilId", S.nullableAsOption(S.string), None),
        folderid: s.fieldOr("folderId", S.nullableAsOption(S.json), None),
        type_: s.fieldOr("type", S.nullableAsOption(S.json), None),
        sort: s.fieldOr("sort", S.nullableAsOption(S.json), None),
      })

    type postDriveFilesResponse = array<ComponentSchemas.Drivefile.t>

    let postDriveFilesResponseSchema = S.array(ComponentSchemas.Drivefile.schema)

    // drive/files
    let postDriveFiles = (~body, ~fetch): promise<postDriveFilesResponse> => {
      let validatedBody = postDriveFilesRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/drive/files",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postDriveFilesResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postDriveFilesAttachedNotesRequest = {
      sinceid: option<string>,
      untilid: option<string>,
      limit: option<int>,
      fileid: string,
    }

    let postDriveFilesAttachedNotesRequestSchema = S.object(s => {
        sinceid: s.fieldOr("sinceId", S.nullableAsOption(S.string), None),
        untilid: s.fieldOr("untilId", S.nullableAsOption(S.string), None),
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(100)), None),
        fileid: s.field("fileId", S.string),
      })

    type postDriveFilesAttachedNotesResponse = array<ComponentSchemas.Note.t>

    let postDriveFilesAttachedNotesResponseSchema = S.array(ComponentSchemas.Note.schema)

    // drive/files/attached-notes
    let postDriveFilesAttachedNotes = (~body, ~fetch): promise<postDriveFilesAttachedNotesResponse> => {
      let validatedBody = postDriveFilesAttachedNotesRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/drive/files/attached-notes",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postDriveFilesAttachedNotesResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postDriveFilesCheckExistenceRequest = {
      md5: string,
    }

    let postDriveFilesCheckExistenceRequestSchema = S.object(s => {
        md5: s.field("md5", S.string),
      })

    type postDriveFilesCheckExistenceResponse = bool

    let postDriveFilesCheckExistenceResponseSchema = S.bool

    // drive/files/check-existence
    let postDriveFilesCheckExistence = (~body, ~fetch): promise<postDriveFilesCheckExistenceResponse> => {
      let validatedBody = postDriveFilesCheckExistenceRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/drive/files/check-existence",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postDriveFilesCheckExistenceResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postDriveFilesCreateRequest = {
      folderid: option<JSON.t>,
      name: option<JSON.t>,
      comment: option<JSON.t>,
      issensitive: option<bool>,
      force: option<bool>,
      file: string,
    }

    let postDriveFilesCreateRequestSchema = S.object(s => {
        folderid: s.fieldOr("folderId", S.nullableAsOption(S.json), None),
        name: s.fieldOr("name", S.nullableAsOption(S.json), None),
        comment: s.fieldOr("comment", S.nullableAsOption(S.json), None),
        issensitive: s.fieldOr("isSensitive", S.nullableAsOption(S.bool), None),
        force: s.fieldOr("force", S.nullableAsOption(S.bool), None),
        file: s.field("file", S.string),
      })

    type postDriveFilesCreateResponse = JSON.t

    let postDriveFilesCreateResponseSchema = S.json

    // drive/files/create
    let postDriveFilesCreate = (~body, ~fetch): promise<postDriveFilesCreateResponse> => {
      let validatedBody = postDriveFilesCreateRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/drive/files/create",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postDriveFilesCreateResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postDriveFilesDeleteRequest = {
      fileid: string,
    }

    let postDriveFilesDeleteRequestSchema = S.object(s => {
        fileid: s.field("fileId", S.string),
      })

    type postDriveFilesDeleteResponse = unit

    // drive/files/delete
    let postDriveFilesDelete = (~body, ~fetch): promise<postDriveFilesDeleteResponse> => {
      let validatedBody = postDriveFilesDeleteRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/drive/files/delete",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postDriveFilesFindRequest = {
      name: string,
      folderid: option<JSON.t>,
    }

    let postDriveFilesFindRequestSchema = S.object(s => {
        name: s.field("name", S.string),
        folderid: s.fieldOr("folderId", S.nullableAsOption(S.json), None),
      })

    type postDriveFilesFindResponse = array<ComponentSchemas.Drivefile.t>

    let postDriveFilesFindResponseSchema = S.array(ComponentSchemas.Drivefile.schema)

    // drive/files/find
    let postDriveFilesFind = (~body, ~fetch): promise<postDriveFilesFindResponse> => {
      let validatedBody = postDriveFilesFindRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/drive/files/find",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postDriveFilesFindResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postDriveFilesFindByHashRequest = {
      md5: string,
    }

    let postDriveFilesFindByHashRequestSchema = S.object(s => {
        md5: s.field("md5", S.string),
      })

    type postDriveFilesFindByHashResponse = array<ComponentSchemas.Drivefile.t>

    let postDriveFilesFindByHashResponseSchema = S.array(ComponentSchemas.Drivefile.schema)

    // drive/files/find-by-hash
    let postDriveFilesFindByHash = (~body, ~fetch): promise<postDriveFilesFindByHashResponse> => {
      let validatedBody = postDriveFilesFindByHashRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/drive/files/find-by-hash",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postDriveFilesFindByHashResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postDriveFilesShowRequest = {
      fileid: option<string>,
      url: option<string>,
    }

    let postDriveFilesShowRequestSchema = S.object(s => {
        fileid: s.fieldOr("fileId", S.nullableAsOption(S.string), None),
        url: s.fieldOr("url", S.nullableAsOption(S.string), None),
      })

    type postDriveFilesShowResponse = JSON.t

    let postDriveFilesShowResponseSchema = S.json

    // drive/files/show
    let postDriveFilesShow = (~body, ~fetch): promise<postDriveFilesShowResponse> => {
      let validatedBody = postDriveFilesShowRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/drive/files/show",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postDriveFilesShowResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postDriveFilesUpdateRequest = {
      fileid: string,
      folderid: option<JSON.t>,
      name: option<string>,
      issensitive: option<bool>,
      comment: option<JSON.t>,
    }

    let postDriveFilesUpdateRequestSchema = S.object(s => {
        fileid: s.field("fileId", S.string),
        folderid: s.fieldOr("folderId", S.nullableAsOption(S.json), None),
        name: s.fieldOr("name", S.nullableAsOption(S.string), None),
        issensitive: s.fieldOr("isSensitive", S.nullableAsOption(S.bool), None),
        comment: s.fieldOr("comment", S.nullableAsOption(S.json), None),
      })

    type postDriveFilesUpdateResponse = JSON.t

    let postDriveFilesUpdateResponseSchema = S.json

    // drive/files/update
    let postDriveFilesUpdate = (~body, ~fetch): promise<postDriveFilesUpdateResponse> => {
      let validatedBody = postDriveFilesUpdateRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/drive/files/update",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postDriveFilesUpdateResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postDriveFilesUploadFromUrlRequest = {
      url: string,
      folderid: option<JSON.t>,
      issensitive: option<bool>,
      comment: option<JSON.t>,
      marker: option<JSON.t>,
      force: option<bool>,
    }

    let postDriveFilesUploadFromUrlRequestSchema = S.object(s => {
        url: s.field("url", S.string),
        folderid: s.fieldOr("folderId", S.nullableAsOption(S.json), None),
        issensitive: s.fieldOr("isSensitive", S.nullableAsOption(S.bool), None),
        comment: s.fieldOr("comment", S.nullableAsOption(S.json), None),
        marker: s.fieldOr("marker", S.nullableAsOption(S.json), None),
        force: s.fieldOr("force", S.nullableAsOption(S.bool), None),
      })

    type postDriveFilesUploadFromUrlResponse = unit

    // drive/files/upload-from-url
    let postDriveFilesUploadFromUrl = (~body, ~fetch): promise<postDriveFilesUploadFromUrlResponse> => {
      let validatedBody = postDriveFilesUploadFromUrlRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/drive/files/upload-from-url",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postDriveFoldersRequest = {
      limit: option<int>,
      sinceid: option<string>,
      untilid: option<string>,
      folderid: option<JSON.t>,
    }

    let postDriveFoldersRequestSchema = S.object(s => {
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(100)), None),
        sinceid: s.fieldOr("sinceId", S.nullableAsOption(S.string), None),
        untilid: s.fieldOr("untilId", S.nullableAsOption(S.string), None),
        folderid: s.fieldOr("folderId", S.nullableAsOption(S.json), None),
      })

    type postDriveFoldersResponse = array<ComponentSchemas.Drivefolder.t>

    let postDriveFoldersResponseSchema = S.array(ComponentSchemas.Drivefolder.schema)

    // drive/folders
    let postDriveFolders = (~body, ~fetch): promise<postDriveFoldersResponse> => {
      let validatedBody = postDriveFoldersRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/drive/folders",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postDriveFoldersResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postDriveFoldersCreateRequest = {
      name: option<string>,
      parentid: option<JSON.t>,
    }

    let postDriveFoldersCreateRequestSchema = S.object(s => {
        name: s.fieldOr("name", S.nullableAsOption(S.string->S.max(200)), None),
        parentid: s.fieldOr("parentId", S.nullableAsOption(S.json), None),
      })

    type postDriveFoldersCreateResponse = JSON.t

    let postDriveFoldersCreateResponseSchema = S.json

    // drive/folders/create
    let postDriveFoldersCreate = (~body, ~fetch): promise<postDriveFoldersCreateResponse> => {
      let validatedBody = postDriveFoldersCreateRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/drive/folders/create",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postDriveFoldersCreateResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postDriveFoldersDeleteRequest = {
      folderid: string,
    }

    let postDriveFoldersDeleteRequestSchema = S.object(s => {
        folderid: s.field("folderId", S.string),
      })

    type postDriveFoldersDeleteResponse = unit

    // drive/folders/delete
    let postDriveFoldersDelete = (~body, ~fetch): promise<postDriveFoldersDeleteResponse> => {
      let validatedBody = postDriveFoldersDeleteRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/drive/folders/delete",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postDriveFoldersFindRequest = {
      name: string,
      parentid: option<JSON.t>,
    }

    let postDriveFoldersFindRequestSchema = S.object(s => {
        name: s.field("name", S.string),
        parentid: s.fieldOr("parentId", S.nullableAsOption(S.json), None),
      })

    type postDriveFoldersFindResponse = array<ComponentSchemas.Drivefolder.t>

    let postDriveFoldersFindResponseSchema = S.array(ComponentSchemas.Drivefolder.schema)

    // drive/folders/find
    let postDriveFoldersFind = (~body, ~fetch): promise<postDriveFoldersFindResponse> => {
      let validatedBody = postDriveFoldersFindRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/drive/folders/find",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postDriveFoldersFindResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postDriveFoldersShowRequest = {
      folderid: string,
    }

    let postDriveFoldersShowRequestSchema = S.object(s => {
        folderid: s.field("folderId", S.string),
      })

    type postDriveFoldersShowResponse = JSON.t

    let postDriveFoldersShowResponseSchema = S.json

    // drive/folders/show
    let postDriveFoldersShow = (~body, ~fetch): promise<postDriveFoldersShowResponse> => {
      let validatedBody = postDriveFoldersShowRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/drive/folders/show",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postDriveFoldersShowResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postDriveFoldersUpdateRequest = {
      folderid: string,
      name: option<string>,
      parentid: option<JSON.t>,
    }

    let postDriveFoldersUpdateRequestSchema = S.object(s => {
        folderid: s.field("folderId", S.string),
        name: s.fieldOr("name", S.nullableAsOption(S.string->S.max(200)), None),
        parentid: s.fieldOr("parentId", S.nullableAsOption(S.json), None),
      })

    type postDriveFoldersUpdateResponse = JSON.t

    let postDriveFoldersUpdateResponseSchema = S.json

    // drive/folders/update
    let postDriveFoldersUpdate = (~body, ~fetch): promise<postDriveFoldersUpdateResponse> => {
      let validatedBody = postDriveFoldersUpdateRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/drive/folders/update",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postDriveFoldersUpdateResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postDriveStreamRequest = {
      limit: option<int>,
      sinceid: option<string>,
      untilid: option<string>,
      @as("type") type_: option<string>,
    }

    let postDriveStreamRequestSchema = S.object(s => {
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(100)), None),
        sinceid: s.fieldOr("sinceId", S.nullableAsOption(S.string), None),
        untilid: s.fieldOr("untilId", S.nullableAsOption(S.string), None),
        type_: s.fieldOr("type", S.nullableAsOption(S.string->S.pattern(%re("/^[a-zA-Z\\/\\-*]+$/"))), None),
      })

    type postDriveStreamResponse = array<ComponentSchemas.Drivefile.t>

    let postDriveStreamResponseSchema = S.array(ComponentSchemas.Drivefile.schema)

    // drive/stream
    let postDriveStream = (~body, ~fetch): promise<postDriveStreamResponse> => {
      let validatedBody = postDriveStreamRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/drive/stream",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postDriveStreamResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

}