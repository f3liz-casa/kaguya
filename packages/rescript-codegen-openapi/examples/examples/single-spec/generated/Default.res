// SPDX-License-Identifier: MIT
// API endpoints for default
// Generated by rescript-codegen-openapi
// DO NOT EDIT - This file is auto-generated



module Default = {
    type postAdminEmojiImportZipRequest = {
      fileid: string,
    }

    let postAdminEmojiImportZipRequestSchema = S.object(s => {
        fileid: s.field("fileId", S.string),
      })

    type postAdminEmojiImportZipResponse = unit

    // admin/emoji/import-zip
    let postAdminEmojiImportZip = (~body, ~fetch): promise<postAdminEmojiImportZipResponse> => {
      let validatedBody = postAdminEmojiImportZipRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/admin/emoji/import-zip",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type getBubbleGameRankingRequest = {
      gamemode: string,
    }

    let getBubbleGameRankingRequestSchema = S.object(s => {
        gamemode: s.field("gameMode", S.string),
      })

    type getBubbleGameRankingResponse = array<JSON.t>

    let getBubbleGameRankingResponseSchema = S.array(S.json)

    // bubble-game/ranking
    let getBubbleGameRanking = (~body, ~fetch): promise<getBubbleGameRankingResponse> => {
      let validatedBody = getBubbleGameRankingRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/bubble-game/ranking",
        ~method_="GET",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = getBubbleGameRankingResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postBubbleGameRankingRequest = {
      gamemode: string,
    }

    let postBubbleGameRankingRequestSchema = S.object(s => {
        gamemode: s.field("gameMode", S.string),
      })

    type postBubbleGameRankingResponse = array<JSON.t>

    let postBubbleGameRankingResponseSchema = S.array(S.json)

    // bubble-game/ranking
    let postBubbleGameRanking = (~body, ~fetch): promise<postBubbleGameRankingResponse> => {
      let validatedBody = postBubbleGameRankingRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/bubble-game/ranking",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postBubbleGameRankingResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postBubbleGameRegisterRequest = {
      score: int,
      seed: string,
      logs: array<array<float>>,
      gamemode: string,
      gameversion: int,
    }

    let postBubbleGameRegisterRequestSchema = S.object(s => {
        score: s.field("score", S.int->S.min(0)),
        seed: s.field("seed", S.string->S.min(1)->S.max(1024)),
        logs: s.field("logs", S.array(S.array(S.float))),
        gamemode: s.field("gameMode", S.string),
        gameversion: s.field("gameVersion", S.int),
      })

    type postBubbleGameRegisterResponse = unit

    // bubble-game/register
    let postBubbleGameRegister = (~body, ~fetch): promise<postBubbleGameRegisterResponse> => {
      let validatedBody = postBubbleGameRegisterRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/bubble-game/register",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postExportCustomEmojisResponse = unit

    // export-custom-emojis
    let postExportCustomEmojis = (~fetch): promise<postExportCustomEmojisResponse> => {

      fetch(
        ~url="/export-custom-emojis",
        ~method_="POST",
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postI2faDoneRequest = {
      token: string,
    }

    let postI2faDoneRequestSchema = S.object(s => {
        token: s.field("token", S.string),
      })

    type postI2faDoneResponse = {
      backupcodes: array<string>,
    }

    let postI2faDoneResponseSchema = S.object(s => {
        backupcodes: s.field("backupCodes", S.array(S.string)),
      })

    // i/2fa/done
    let postI2faDone = (~body, ~fetch): promise<postI2faDoneResponse> => {
      let validatedBody = postI2faDoneRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/2fa/done",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postI2faDoneResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postI2faKeyDoneRequest = {
      password: string,
      token: option<JSON.t>,
      name: string,
      credential: JSON.t,
    }

    let postI2faKeyDoneRequestSchema = S.object(s => {
        password: s.field("password", S.string),
        token: s.fieldOr("token", S.nullableAsOption(S.json), None),
        name: s.field("name", S.string->S.min(1)->S.max(30)),
        credential: s.field("credential", S.json),
      })

    type postI2faKeyDoneResponse = {
      id: string,
      name: string,
    }

    let postI2faKeyDoneResponseSchema = S.object(s => {
        id: s.field("id", S.string),
        name: s.field("name", S.string),
      })

    // i/2fa/key-done
    let postI2faKeyDone = (~body, ~fetch): promise<postI2faKeyDoneResponse> => {
      let validatedBody = postI2faKeyDoneRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/2fa/key-done",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postI2faKeyDoneResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postI2faPasswordLessRequest = {
      value: bool,
    }

    let postI2faPasswordLessRequestSchema = S.object(s => {
        value: s.field("value", S.bool),
      })

    type postI2faPasswordLessResponse = unit

    // i/2fa/password-less
    let postI2faPasswordLess = (~body, ~fetch): promise<postI2faPasswordLessResponse> => {
      let validatedBody = postI2faPasswordLessRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/2fa/password-less",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postI2faRegisterRequest = {
      password: string,
      token: option<JSON.t>,
    }

    let postI2faRegisterRequestSchema = S.object(s => {
        password: s.field("password", S.string),
        token: s.fieldOr("token", S.nullableAsOption(S.json), None),
      })

    type postI2faRegisterResponse = {
      qr: string,
      url: string,
      secret: string,
      label: string,
      issuer: string,
    }

    let postI2faRegisterResponseSchema = S.object(s => {
        qr: s.field("qr", S.string),
        url: s.field("url", S.string),
        secret: s.field("secret", S.string),
        label: s.field("label", S.string),
        issuer: s.field("issuer", S.string),
      })

    // i/2fa/register
    let postI2faRegister = (~body, ~fetch): promise<postI2faRegisterResponse> => {
      let validatedBody = postI2faRegisterRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/2fa/register",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postI2faRegisterResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postI2faRegisterKeyRequest = {
      password: string,
      token: option<JSON.t>,
    }

    let postI2faRegisterKeyRequestSchema = S.object(s => {
        password: s.field("password", S.string),
        token: s.fieldOr("token", S.nullableAsOption(S.json), None),
      })

    type postI2faRegisterKeyResponse = {
      rp: JSON.t,
      user: JSON.t,
      challenge: string,
      pubkeycredparams: array<JSON.t>,
      timeout: JSON.t,
      excludecredentials: JSON.t,
      authenticatorselection: JSON.t,
      attestation: JSON.t,
      extensions: JSON.t,
    }

    let postI2faRegisterKeyResponseSchema = S.object(s => {
        rp: s.field("rp", S.json),
        user: s.field("user", S.json),
        challenge: s.field("challenge", S.string),
        pubkeycredparams: s.field("pubKeyCredParams", S.array(S.json)),
        timeout: s.field("timeout", S.json),
        excludecredentials: s.field("excludeCredentials", S.json),
        authenticatorselection: s.field("authenticatorSelection", S.json),
        attestation: s.field("attestation", S.json),
        extensions: s.field("extensions", S.json),
      })

    // i/2fa/register-key
    let postI2faRegisterKey = (~body, ~fetch): promise<postI2faRegisterKeyResponse> => {
      let validatedBody = postI2faRegisterKeyRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/2fa/register-key",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postI2faRegisterKeyResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postI2faRemoveKeyRequest = {
      password: string,
      token: option<JSON.t>,
      credentialid: string,
    }

    let postI2faRemoveKeyRequestSchema = S.object(s => {
        password: s.field("password", S.string),
        token: s.fieldOr("token", S.nullableAsOption(S.json), None),
        credentialid: s.field("credentialId", S.string),
      })

    type postI2faRemoveKeyResponse = unit

    // i/2fa/remove-key
    let postI2faRemoveKey = (~body, ~fetch): promise<postI2faRemoveKeyResponse> => {
      let validatedBody = postI2faRemoveKeyRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/2fa/remove-key",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postI2faUnregisterRequest = {
      password: string,
      token: option<JSON.t>,
    }

    let postI2faUnregisterRequestSchema = S.object(s => {
        password: s.field("password", S.string),
        token: s.fieldOr("token", S.nullableAsOption(S.json), None),
      })

    type postI2faUnregisterResponse = unit

    // i/2fa/unregister
    let postI2faUnregister = (~body, ~fetch): promise<postI2faUnregisterResponse> => {
      let validatedBody = postI2faUnregisterRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/2fa/unregister",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postI2faUpdateKeyRequest = {
      name: string,
      credentialid: string,
    }

    let postI2faUpdateKeyRequestSchema = S.object(s => {
        name: s.field("name", S.string->S.min(1)->S.max(30)),
        credentialid: s.field("credentialId", S.string),
      })

    type postI2faUpdateKeyResponse = unit

    // i/2fa/update-key
    let postI2faUpdateKey = (~body, ~fetch): promise<postI2faUpdateKeyResponse> => {
      let validatedBody = postI2faUpdateKeyRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/2fa/update-key",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postIAppsRequest = {
      sort: option<string>,
    }

    let postIAppsRequestSchema = S.object(s => {
        sort: s.fieldOr("sort", S.nullableAsOption(S.string), None),
      })

    type postIAppsResponse = array<JSON.t>

    let postIAppsResponseSchema = S.array(S.json)

    // i/apps
    let postIApps = (~body, ~fetch): promise<postIAppsResponse> => {
      let validatedBody = postIAppsRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/apps",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postIAppsResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postIAuthorizedAppsRequest = {
      limit: option<int>,
      offset: option<int>,
      sort: option<string>,
    }

    let postIAuthorizedAppsRequestSchema = S.object(s => {
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(100)), None),
        offset: s.fieldOr("offset", S.nullableAsOption(S.int), None),
        sort: s.fieldOr("sort", S.nullableAsOption(S.string), None),
      })

    type postIAuthorizedAppsResponse = array<JSON.t>

    let postIAuthorizedAppsResponseSchema = S.array(S.json)

    // i/authorized-apps
    let postIAuthorizedApps = (~body, ~fetch): promise<postIAuthorizedAppsResponse> => {
      let validatedBody = postIAuthorizedAppsRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/authorized-apps",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postIAuthorizedAppsResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postIChangePasswordRequest = {
      currentpassword: string,
      newpassword: string,
      token: option<JSON.t>,
    }

    let postIChangePasswordRequestSchema = S.object(s => {
        currentpassword: s.field("currentPassword", S.string),
        newpassword: s.field("newPassword", S.string->S.min(1)),
        token: s.fieldOr("token", S.nullableAsOption(S.json), None),
      })

    type postIChangePasswordResponse = unit

    // i/change-password
    let postIChangePassword = (~body, ~fetch): promise<postIChangePasswordResponse> => {
      let validatedBody = postIChangePasswordRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/change-password",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postIClaimAchievementRequest = {
      name: string,
    }

    let postIClaimAchievementRequestSchema = S.object(s => {
        name: s.field("name", S.string),
      })

    type postIClaimAchievementResponse = unit

    // i/claim-achievement
    let postIClaimAchievement = (~body, ~fetch): promise<postIClaimAchievementResponse> => {
      let validatedBody = postIClaimAchievementRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/claim-achievement",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postIDeleteAccountRequest = {
      password: string,
      token: option<JSON.t>,
    }

    let postIDeleteAccountRequestSchema = S.object(s => {
        password: s.field("password", S.string),
        token: s.fieldOr("token", S.nullableAsOption(S.json), None),
      })

    type postIDeleteAccountResponse = unit

    // i/delete-account
    let postIDeleteAccount = (~body, ~fetch): promise<postIDeleteAccountResponse> => {
      let validatedBody = postIDeleteAccountRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/delete-account",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postIExportAntennasResponse = unit

    // i/export-antennas
    let postIExportAntennas = (~fetch): promise<postIExportAntennasResponse> => {

      fetch(
        ~url="/i/export-antennas",
        ~method_="POST",
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postIExportBlockingResponse = unit

    // i/export-blocking
    let postIExportBlocking = (~fetch): promise<postIExportBlockingResponse> => {

      fetch(
        ~url="/i/export-blocking",
        ~method_="POST",
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postIExportClipsResponse = unit

    // i/export-clips
    let postIExportClips = (~fetch): promise<postIExportClipsResponse> => {

      fetch(
        ~url="/i/export-clips",
        ~method_="POST",
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postIExportFavoritesResponse = unit

    // i/export-favorites
    let postIExportFavorites = (~fetch): promise<postIExportFavoritesResponse> => {

      fetch(
        ~url="/i/export-favorites",
        ~method_="POST",
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postIExportFollowingRequest = {
      excludemuting: option<bool>,
      excludeinactive: option<bool>,
    }

    let postIExportFollowingRequestSchema = S.object(s => {
        excludemuting: s.fieldOr("excludeMuting", S.nullableAsOption(S.bool), None),
        excludeinactive: s.fieldOr("excludeInactive", S.nullableAsOption(S.bool), None),
      })

    type postIExportFollowingResponse = unit

    // i/export-following
    let postIExportFollowing = (~body, ~fetch): promise<postIExportFollowingResponse> => {
      let validatedBody = postIExportFollowingRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/export-following",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postIExportMuteResponse = unit

    // i/export-mute
    let postIExportMute = (~fetch): promise<postIExportMuteResponse> => {

      fetch(
        ~url="/i/export-mute",
        ~method_="POST",
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postIExportNotesResponse = unit

    // i/export-notes
    let postIExportNotes = (~fetch): promise<postIExportNotesResponse> => {

      fetch(
        ~url="/i/export-notes",
        ~method_="POST",
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postIExportUserListsResponse = unit

    // i/export-user-lists
    let postIExportUserLists = (~fetch): promise<postIExportUserListsResponse> => {

      fetch(
        ~url="/i/export-user-lists",
        ~method_="POST",
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postIImportAntennasRequest = {
      fileid: string,
    }

    let postIImportAntennasRequestSchema = S.object(s => {
        fileid: s.field("fileId", S.string),
      })

    type postIImportAntennasResponse = unit

    // i/import-antennas
    let postIImportAntennas = (~body, ~fetch): promise<postIImportAntennasResponse> => {
      let validatedBody = postIImportAntennasRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/import-antennas",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postIImportBlockingRequest = {
      fileid: string,
    }

    let postIImportBlockingRequestSchema = S.object(s => {
        fileid: s.field("fileId", S.string),
      })

    type postIImportBlockingResponse = unit

    // i/import-blocking
    let postIImportBlocking = (~body, ~fetch): promise<postIImportBlockingResponse> => {
      let validatedBody = postIImportBlockingRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/import-blocking",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postIImportFollowingRequest = {
      fileid: string,
      withreplies: option<bool>,
    }

    let postIImportFollowingRequestSchema = S.object(s => {
        fileid: s.field("fileId", S.string),
        withreplies: s.fieldOr("withReplies", S.nullableAsOption(S.bool), None),
      })

    type postIImportFollowingResponse = unit

    // i/import-following
    let postIImportFollowing = (~body, ~fetch): promise<postIImportFollowingResponse> => {
      let validatedBody = postIImportFollowingRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/import-following",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postIImportMutingRequest = {
      fileid: string,
    }

    let postIImportMutingRequestSchema = S.object(s => {
        fileid: s.field("fileId", S.string),
      })

    type postIImportMutingResponse = unit

    // i/import-muting
    let postIImportMuting = (~body, ~fetch): promise<postIImportMutingResponse> => {
      let validatedBody = postIImportMutingRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/import-muting",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postIImportUserListsRequest = {
      fileid: string,
    }

    let postIImportUserListsRequestSchema = S.object(s => {
        fileid: s.field("fileId", S.string),
      })

    type postIImportUserListsResponse = unit

    // i/import-user-lists
    let postIImportUserLists = (~body, ~fetch): promise<postIImportUserListsResponse> => {
      let validatedBody = postIImportUserListsRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/import-user-lists",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postIRegenerateTokenRequest = {
      password: string,
    }

    let postIRegenerateTokenRequestSchema = S.object(s => {
        password: s.field("password", S.string),
      })

    type postIRegenerateTokenResponse = unit

    // i/regenerate-token
    let postIRegenerateToken = (~body, ~fetch): promise<postIRegenerateTokenResponse> => {
      let validatedBody = postIRegenerateTokenRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/regenerate-token",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postIRegistryGetRequest = {
      key: string,
      scope: array<string>,
      domain: option<JSON.t>,
    }

    let postIRegistryGetRequestSchema = S.object(s => {
        key: s.field("key", S.string),
        scope: s.field("scope", S.array(S.string->S.pattern(%re("/^[a-zA-Z0-9_]+$/")))),
        domain: s.fieldOr("domain", S.nullableAsOption(S.json), None),
      })

    type postIRegistryGetResponse = JSON.t

    let postIRegistryGetResponseSchema = S.json

    // i/registry/get
    let postIRegistryGet = (~body, ~fetch): promise<postIRegistryGetResponse> => {
      let validatedBody = postIRegistryGetRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/registry/get",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postIRegistryGetResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postIRegistryGetAllRequest = {
      scope: array<string>,
      domain: option<JSON.t>,
    }

    let postIRegistryGetAllRequestSchema = S.object(s => {
        scope: s.field("scope", S.array(S.string->S.pattern(%re("/^[a-zA-Z0-9_]+$/")))),
        domain: s.fieldOr("domain", S.nullableAsOption(S.json), None),
      })

    type postIRegistryGetAllResponse = JSON.t

    let postIRegistryGetAllResponseSchema = S.json

    // i/registry/get-all
    let postIRegistryGetAll = (~body, ~fetch): promise<postIRegistryGetAllResponse> => {
      let validatedBody = postIRegistryGetAllRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/registry/get-all",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postIRegistryGetAllResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postIRegistryGetDetailRequest = {
      key: string,
      scope: array<string>,
      domain: option<JSON.t>,
    }

    let postIRegistryGetDetailRequestSchema = S.object(s => {
        key: s.field("key", S.string),
        scope: s.field("scope", S.array(S.string->S.pattern(%re("/^[a-zA-Z0-9_]+$/")))),
        domain: s.fieldOr("domain", S.nullableAsOption(S.json), None),
      })

    type postIRegistryGetDetailResponse = {
      updatedat: string,
      value: JSON.t,
    }

    let postIRegistryGetDetailResponseSchema = S.object(s => {
        updatedat: s.field("updatedAt", S.string),
        value: s.field("value", S.json),
      })

    // i/registry/get-detail
    let postIRegistryGetDetail = (~body, ~fetch): promise<postIRegistryGetDetailResponse> => {
      let validatedBody = postIRegistryGetDetailRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/registry/get-detail",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postIRegistryGetDetailResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postIRegistryKeysRequest = {
      scope: array<string>,
      domain: option<JSON.t>,
    }

    let postIRegistryKeysRequestSchema = S.object(s => {
        scope: s.field("scope", S.array(S.string->S.pattern(%re("/^[a-zA-Z0-9_]+$/")))),
        domain: s.fieldOr("domain", S.nullableAsOption(S.json), None),
      })

    type postIRegistryKeysResponse = array<string>

    let postIRegistryKeysResponseSchema = S.array(S.string)

    // i/registry/keys
    let postIRegistryKeys = (~body, ~fetch): promise<postIRegistryKeysResponse> => {
      let validatedBody = postIRegistryKeysRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/registry/keys",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postIRegistryKeysResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postIRegistryKeysWithTypeRequest = {
      scope: array<string>,
      domain: option<JSON.t>,
    }

    let postIRegistryKeysWithTypeRequestSchema = S.object(s => {
        scope: s.field("scope", S.array(S.string->S.pattern(%re("/^[a-zA-Z0-9_]+$/")))),
        domain: s.fieldOr("domain", S.nullableAsOption(S.json), None),
      })

    type postIRegistryKeysWithTypeResponse = JSON.t

    let postIRegistryKeysWithTypeResponseSchema = S.json

    // i/registry/keys-with-type
    let postIRegistryKeysWithType = (~body, ~fetch): promise<postIRegistryKeysWithTypeResponse> => {
      let validatedBody = postIRegistryKeysWithTypeRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/registry/keys-with-type",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postIRegistryKeysWithTypeResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postIRegistryRemoveRequest = {
      key: string,
      scope: array<string>,
      domain: option<JSON.t>,
    }

    let postIRegistryRemoveRequestSchema = S.object(s => {
        key: s.field("key", S.string),
        scope: s.field("scope", S.array(S.string->S.pattern(%re("/^[a-zA-Z0-9_]+$/")))),
        domain: s.fieldOr("domain", S.nullableAsOption(S.json), None),
      })

    type postIRegistryRemoveResponse = unit

    // i/registry/remove
    let postIRegistryRemove = (~body, ~fetch): promise<postIRegistryRemoveResponse> => {
      let validatedBody = postIRegistryRemoveRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/registry/remove",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postIRegistryScopesWithDomainResponse = array<JSON.t>

    let postIRegistryScopesWithDomainResponseSchema = S.array(S.json)

    // i/registry/scopes-with-domain
    let postIRegistryScopesWithDomain = (~fetch): promise<postIRegistryScopesWithDomainResponse> => {

      fetch(
        ~url="/i/registry/scopes-with-domain",
        ~method_="POST",
      )->Promise.then(response => {
      let validatedResponse = postIRegistryScopesWithDomainResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postIRegistrySetRequest = {
      key: string,
      value: JSON.t,
      scope: array<string>,
      domain: option<JSON.t>,
    }

    let postIRegistrySetRequestSchema = S.object(s => {
        key: s.field("key", S.string->S.min(1)),
        value: s.field("value", S.json),
        scope: s.field("scope", S.array(S.string->S.pattern(%re("/^[a-zA-Z0-9_]+$/")))),
        domain: s.fieldOr("domain", S.nullableAsOption(S.json), None),
      })

    type postIRegistrySetResponse = unit

    // i/registry/set
    let postIRegistrySet = (~body, ~fetch): promise<postIRegistrySetResponse> => {
      let validatedBody = postIRegistrySetRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/registry/set",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postIRevokeTokenRequest = {
      tokenid: option<string>,
      token: option<JSON.t>,
    }

    let postIRevokeTokenRequestSchema = S.object(s => {
        tokenid: s.fieldOr("tokenId", S.nullableAsOption(S.string), None),
        token: s.fieldOr("token", S.nullableAsOption(S.json), None),
      })

    type postIRevokeTokenResponse = unit

    // i/revoke-token
    let postIRevokeToken = (~body, ~fetch): promise<postIRevokeTokenResponse> => {
      let validatedBody = postIRevokeTokenRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/revoke-token",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postISigninHistoryRequest = {
      limit: option<int>,
      sinceid: option<string>,
      untilid: option<string>,
    }

    let postISigninHistoryRequestSchema = S.object(s => {
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(100)), None),
        sinceid: s.fieldOr("sinceId", S.nullableAsOption(S.string), None),
        untilid: s.fieldOr("untilId", S.nullableAsOption(S.string), None),
      })

    type postISigninHistoryResponse = array<ComponentSchemas.Signin.t>

    let postISigninHistoryResponseSchema = S.array(ComponentSchemas.Signin.schema)

    // i/signin-history
    let postISigninHistory = (~body, ~fetch): promise<postISigninHistoryResponse> => {
      let validatedBody = postISigninHistoryRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/signin-history",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postISigninHistoryResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postIUpdateEmailRequest = {
      password: string,
      email: option<JSON.t>,
      token: option<JSON.t>,
    }

    let postIUpdateEmailRequestSchema = S.object(s => {
        password: s.field("password", S.string),
        email: s.fieldOr("email", S.nullableAsOption(S.json), None),
        token: s.fieldOr("token", S.nullableAsOption(S.json), None),
      })

    type postIUpdateEmailResponse = JSON.t

    let postIUpdateEmailResponseSchema = S.json

    // i/update-email
    let postIUpdateEmail = (~body, ~fetch): promise<postIUpdateEmailResponse> => {
      let validatedBody = postIUpdateEmailRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/i/update-email",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postIUpdateEmailResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postPagePushRequest = {
      pageid: string,
      event: string,
      var: option<JSON.t>,
    }

    let postPagePushRequestSchema = S.object(s => {
        pageid: s.field("pageId", S.string),
        event: s.field("event", S.string),
        var: s.fieldOr("var", S.nullableAsOption(S.json), None),
      })

    type postPagePushResponse = unit

    // page-push
    let postPagePush = (~body, ~fetch): promise<postPagePushResponse> => {
      let validatedBody = postPagePushRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/page-push",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postReversiCancelMatchRequest = {
      userid: option<JSON.t>,
    }

    let postReversiCancelMatchRequestSchema = S.object(s => {
        userid: s.fieldOr("userId", S.nullableAsOption(S.json), None),
      })

    type postReversiCancelMatchResponse = unit

    // reversi/cancel-match
    let postReversiCancelMatch = (~body, ~fetch): promise<postReversiCancelMatchResponse> => {
      let validatedBody = postReversiCancelMatchRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/reversi/cancel-match",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postReversiGamesRequest = {
      limit: option<int>,
      sinceid: option<string>,
      untilid: option<string>,
      my: option<bool>,
    }

    let postReversiGamesRequestSchema = S.object(s => {
        limit: s.fieldOr("limit", S.nullableAsOption(S.int->S.min(1)->S.max(100)), None),
        sinceid: s.fieldOr("sinceId", S.nullableAsOption(S.string), None),
        untilid: s.fieldOr("untilId", S.nullableAsOption(S.string), None),
        my: s.fieldOr("my", S.nullableAsOption(S.bool), None),
      })

    type postReversiGamesResponse = array<ComponentSchemas.Reversigamelite.t>

    let postReversiGamesResponseSchema = S.array(ComponentSchemas.Reversigamelite.schema)

    // reversi/games
    let postReversiGames = (~body, ~fetch): promise<postReversiGamesResponse> => {
      let validatedBody = postReversiGamesRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/reversi/games",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postReversiGamesResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postReversiInvitationsResponse = array<ComponentSchemas.Userlite.t>

    let postReversiInvitationsResponseSchema = S.array(ComponentSchemas.Userlite.schema)

    // reversi/invitations
    let postReversiInvitations = (~fetch): promise<postReversiInvitationsResponse> => {

      fetch(
        ~url="/reversi/invitations",
        ~method_="POST",
      )->Promise.then(response => {
      let validatedResponse = postReversiInvitationsResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postReversiMatchRequest = {
      userid: option<JSON.t>,
      noirregularrules: option<bool>,
      multiple: option<bool>,
    }

    let postReversiMatchRequestSchema = S.object(s => {
        userid: s.fieldOr("userId", S.nullableAsOption(S.json), None),
        noirregularrules: s.fieldOr("noIrregularRules", S.nullableAsOption(S.bool), None),
        multiple: s.fieldOr("multiple", S.nullableAsOption(S.bool), None),
      })

    type postReversiMatchResponse = JSON.t

    let postReversiMatchResponseSchema = S.json

    // reversi/match
    let postReversiMatch = (~body, ~fetch): promise<postReversiMatchResponse> => {
      let validatedBody = postReversiMatchRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/reversi/match",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postReversiMatchResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postReversiShowGameRequest = {
      gameid: string,
    }

    let postReversiShowGameRequestSchema = S.object(s => {
        gameid: s.field("gameId", S.string),
      })

    type postReversiShowGameResponse = JSON.t

    let postReversiShowGameResponseSchema = S.json

    // reversi/show-game
    let postReversiShowGame = (~body, ~fetch): promise<postReversiShowGameResponse> => {
      let validatedBody = postReversiShowGameRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/reversi/show-game",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postReversiShowGameResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postReversiSurrenderRequest = {
      gameid: string,
    }

    let postReversiSurrenderRequestSchema = S.object(s => {
        gameid: s.field("gameId", S.string),
      })

    type postReversiSurrenderResponse = unit

    // reversi/surrender
    let postReversiSurrender = (~body, ~fetch): promise<postReversiSurrenderResponse> => {
      let validatedBody = postReversiSurrenderRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/reversi/surrender",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postReversiVerifyRequest = {
      gameid: string,
      crc32: string,
    }

    let postReversiVerifyRequestSchema = S.object(s => {
        gameid: s.field("gameId", S.string),
        crc32: s.field("crc32", S.string),
      })

    type postReversiVerifyResponse = {
      desynced: bool,
      game: option<JSON.t>,
    }

    let postReversiVerifyResponseSchema = S.object(s => {
        desynced: s.field("desynced", S.bool),
        game: s.fieldOr("game", S.nullableAsOption(S.json), None),
      })

    // reversi/verify
    let postReversiVerify = (~body, ~fetch): promise<postReversiVerifyResponse> => {
      let validatedBody = postReversiVerifyRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/reversi/verify",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postReversiVerifyResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postUsersAchievementsRequest = {
      userid: string,
    }

    let postUsersAchievementsRequestSchema = S.object(s => {
        userid: s.field("userId", S.string),
      })

    type postUsersAchievementsResponse = array<JSON.t>

    let postUsersAchievementsResponseSchema = S.array(S.json)

    // users/achievements
    let postUsersAchievements = (~body, ~fetch): promise<postUsersAchievementsResponse> => {
      let validatedBody = postUsersAchievementsRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/users/achievements",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postUsersAchievementsResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postUsersListsCreateFromPublicRequest = {
      name: string,
      listid: string,
    }

    let postUsersListsCreateFromPublicRequestSchema = S.object(s => {
        name: s.field("name", S.string->S.min(1)->S.max(100)),
        listid: s.field("listId", S.string),
      })

    type postUsersListsCreateFromPublicResponse = JSON.t

    let postUsersListsCreateFromPublicResponseSchema = S.json

    // users/lists/create-from-public
    let postUsersListsCreateFromPublic = (~body, ~fetch): promise<postUsersListsCreateFromPublicResponse> => {
      let validatedBody = postUsersListsCreateFromPublicRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/users/lists/create-from-public",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postUsersListsCreateFromPublicResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postUsersListsFavoriteRequest = {
      listid: string,
    }

    let postUsersListsFavoriteRequestSchema = S.object(s => {
        listid: s.field("listId", S.string),
      })

    type postUsersListsFavoriteResponse = unit

    // users/lists/favorite
    let postUsersListsFavorite = (~body, ~fetch): promise<postUsersListsFavoriteResponse> => {
      let validatedBody = postUsersListsFavoriteRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/users/lists/favorite",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postUsersListsUnfavoriteRequest = {
      listid: string,
    }

    let postUsersListsUnfavoriteRequestSchema = S.object(s => {
        listid: s.field("listId", S.string),
      })

    type postUsersListsUnfavoriteResponse = unit

    // users/lists/unfavorite
    let postUsersListsUnfavorite = (~body, ~fetch): promise<postUsersListsUnfavoriteResponse> => {
      let validatedBody = postUsersListsUnfavoriteRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/users/lists/unfavorite",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

}