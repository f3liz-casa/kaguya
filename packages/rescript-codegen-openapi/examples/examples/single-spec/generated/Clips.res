// SPDX-License-Identifier: MIT
// API endpoints for clips
// Generated by rescript-codegen-openapi
// DO NOT EDIT - This file is auto-generated



module Clips = {
    type postClipsCreateRequest = {
      name: string,
      ispublic: option<bool>,
      description: option<JSON.t>,
    }

    let postClipsCreateRequestSchema = S.object(s => {
        name: s.field("name", S.string->S.min(1)->S.max(100)),
        ispublic: s.fieldOr("isPublic", S.nullableAsOption(S.bool), None),
        description: s.fieldOr("description", S.nullableAsOption(S.json), None),
      })

    type postClipsCreateResponse = JSON.t

    let postClipsCreateResponseSchema = S.json

    // clips/create
    let postClipsCreate = (~body, ~fetch): promise<postClipsCreateResponse> => {
      let validatedBody = postClipsCreateRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/clips/create",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postClipsCreateResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postClipsDeleteRequest = {
      clipid: string,
    }

    let postClipsDeleteRequestSchema = S.object(s => {
        clipid: s.field("clipId", S.string),
      })

    type postClipsDeleteResponse = unit

    // clips/delete
    let postClipsDelete = (~body, ~fetch): promise<postClipsDeleteResponse> => {
      let validatedBody = postClipsDeleteRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/clips/delete",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      response
    ->Promise.resolve
      })
    }

    type postClipsListResponse = array<ComponentSchemas.Clip.t>

    let postClipsListResponseSchema = S.array(ComponentSchemas.Clip.schema)

    // clips/list
    let postClipsList = (~fetch): promise<postClipsListResponse> => {

      fetch(
        ~url="/clips/list",
        ~method_="POST",
      )->Promise.then(response => {
      let validatedResponse = postClipsListResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postClipsShowRequest = {
      clipid: string,
    }

    let postClipsShowRequestSchema = S.object(s => {
        clipid: s.field("clipId", S.string),
      })

    type postClipsShowResponse = JSON.t

    let postClipsShowResponseSchema = S.json

    // clips/show
    let postClipsShow = (~body, ~fetch): promise<postClipsShowResponse> => {
      let validatedBody = postClipsShowRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/clips/show",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postClipsShowResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postClipsUpdateRequest = {
      clipid: string,
      name: option<string>,
      ispublic: option<bool>,
      description: option<JSON.t>,
    }

    let postClipsUpdateRequestSchema = S.object(s => {
        clipid: s.field("clipId", S.string),
        name: s.fieldOr("name", S.nullableAsOption(S.string->S.min(1)->S.max(100)), None),
        ispublic: s.fieldOr("isPublic", S.nullableAsOption(S.bool), None),
        description: s.fieldOr("description", S.nullableAsOption(S.json), None),
      })

    type postClipsUpdateResponse = JSON.t

    let postClipsUpdateResponseSchema = S.json

    // clips/update
    let postClipsUpdate = (~body, ~fetch): promise<postClipsUpdateResponse> => {
      let validatedBody = postClipsUpdateRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/clips/update",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postClipsUpdateResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

    type postNotesClipsRequest = {
      noteid: string,
    }

    let postNotesClipsRequestSchema = S.object(s => {
        noteid: s.field("noteId", S.string),
      })

    type postNotesClipsResponse = array<ComponentSchemas.Clip.t>

    let postNotesClipsResponseSchema = S.array(ComponentSchemas.Clip.schema)

    // notes/clips
    let postNotesClips = (~body, ~fetch): promise<postNotesClipsResponse> => {
      let validatedBody = postNotesClipsRequestSchema->S.parseOrThrow(body)

      fetch(
        ~url="/notes/clips",
        ~method_="POST",
        ~body=validatedBody,
      )->Promise.then(response => {
      let validatedResponse = postNotesClipsResponseSchema->S.parseOrThrow(response)
      validatedResponse
    ->Promise.resolve
      })
    }

}